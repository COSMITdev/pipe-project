{
  "name": "basecamp-clone",
  "scripts": {
    "postdeploy": "bin/rake db:migrate"
  },
  "env": {
    "RACK_ENV": {
      "required": true
    },
    "RAILS_ENV": {
      "required": true
    },
    "RAILS_SERVE_STATIC_FILES": {
      "required": true
    },
    "SECRET_KEY_BASE": {
      "required": true
    }
  },
  "formation": {},
  "addons": [
    "sendgrid",
    "heroku-postgresql"
  ],
  "buildpacks": []
}
